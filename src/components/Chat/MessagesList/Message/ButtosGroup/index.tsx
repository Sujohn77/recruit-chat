import React from 'react';
import { CHAT_ACTIONS, ILocalMessage, MessageType } from 'utils/types';
type PropsType = {
  message: ILocalMessage;
};

export const ButtonsGroup = ({ message }: PropsType) => {
  return null;
};
