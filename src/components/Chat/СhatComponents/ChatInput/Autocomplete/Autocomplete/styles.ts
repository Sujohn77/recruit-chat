import styled from "styled-components";

export const PhoneInputWrapper = styled.div`
  margin-left: 2px;

  & ul {
    position: fixed;
    top: 350px;
    left: 25px;
  }

  & input {
    width: 240px;
  }
`;
